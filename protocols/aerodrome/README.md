# Aerodrome

**Автор:** [Роман Ярлыков](https://github.com/rlkvrv) 🧐

План статьи:
- ✅ Введение (что за протокол)
- Основа - Uniswap v2
- Что взяли от Curve
- Что взяли от Convex
- Заключение
- Ссылки

## Введение

Aerodrome - это децентрализованный обменник (DEX) в сети Base на базе AMM, команда которой попыталась объединить проверенные механики из трех известных протоколов DeFi: Uniswap, Curve и Convex.

Также, Aerodrome - это форк протокола Velodrome v2 в сети Optimism, который сделан той же командой, но запущен с новыми токенами и с обновленной токеномикой. Были токены VELO и veVELO, стали токены AERO и veAERO. При этом держатели токена veVELO получили 40% эмиссии токена AERO (это 200 млн. токенов). Поэтому, почти все что касается протокола Aerodrome будет также справедливо для Velodrome v2.

## Основа - Uniswap v2

Протокол Velodrome v2 запущен в июне 2023 года, это значит, что на тот момент уже два года существовал Uniswap v3 с концентрированной ликвидностью, но тем не менее за основу был взят Uniswap v2, который был адаптирован под современные реалии.

### StableSwap

Uniswap v2 дает возможность создавать так называемые "волатильные" пулы, то есть пулы в которых цена может меняться очень сильно, что совсем не подходило для стейблкоинов (токенов с привязкой к фиксированной цене). Это один из самых больших недостатков протокола, поэтому, первое изменение, которое можно увидеть в Aerodrome/Velodrome v2 - добавлена возможность создавать пулы со стабильными парами.

Такой функционал называется инвариантом `StableSwap`, он позаимствован из протокола Curve. Ранее мы уже писали про Curve и `StableSwap`, если коротко - вместо формулы постоянного произведения `x * y >= k` используется гибридная формула `x3y+y3x >= k`, которая включает с себя сразу два инварианта: постоянного произведения и постоянной суммы. Когда пул стабилен, то есть находится очень близко к целевой стоимости - работает инварианта постоянной суммы, а если пул нестабилен включается инварианта постоянного произведения.

Стальные пулы сразу создаются с флагом "stable", который влияет на все дальнейшие расчеты цены. Если этот флаг не указан, значит это обычный волатильный пул, который использует старую добрую формулу AMM - `x * y >= k`. Таким образом в протоколе появилась возможность создавать как "стабильные" пулы так и классические "волатильные".

### Комиссии

Работа с комиссиями изменилась существенно по отношению к Uniswap v2:

1. Комиссии могут изменяться в диапазоне от самой минимальной (больше 0) до 3%. Задаются специальной ролью на контракте `PoolFactory`. Напомню, что в Uniswap v2 она фиксированная (0.3%)
2. В отличие от Uniswap v2 комиссии отправляются на отдельный смарт-контракт.
3. Из-за второго пункта изменена логика работы с комиссией.

// TODO расписать по каждому пункту

По умолчанию для стабильных пар задана комиссия 0.05%, а для волатильных 0.3%.



## Что взяли от Curve

## Что взяли от Convex

## Заключение

## Ссылки

- []()